<li>
    {{burger_name}}

    {{#unless devoured}}
        <button class="devour" type="button" name="action" data-id="{{id}}" data-eaten="{{devoured}}">
            DEVOUR!
        </button>
    {{/unless}}
</li>